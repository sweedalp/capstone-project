# Main LMS Backend
VITE_API_BASE_URL=http://localhost:8000

# AI Video Chat Agent (WebSocket)
VITE_AI_VIDEO_WS_URL=ws://localhost:8002/ws/chat
VITE_AI_VIDEO_API_URL=http://localhost:8002

# Whisper Agent (used indirectly via main backend proxy)
# Frontend does NOT call this directly — main backend proxies.
# Listed here for reference only.
# VITE_WHISPER_AGENT_URL=http://localhost:8001